
- Set up registering for account
    - Check if name is given
    - Check if name is taken
    - Check if password is given
    - Check ifconfirmation is given
    - Check if confirmation and password are the same
    
- Set up signing in
    - Check if name is given
    - Check if password is given
    - 